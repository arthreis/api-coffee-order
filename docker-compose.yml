version: "3"

services:
    mongo:
        container_name: mongo-cafe
        image: mongo:latest
        volumes:
            - mongodata:/data/db
        ports:
            - 27017:27017
volumes:
  mongodata:

