version: "3"

services:
    api:
        build: .
        command: npm run start:local
        ports:
            - "3000:3000"
    mongo:
        container_name: mongo-cafe
        image: mongo:latest
        volumes:
            - mongodata:/data/db
        ports:
            - 27017:27017
volumes:
  mongodata:

